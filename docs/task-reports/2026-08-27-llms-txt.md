# llms.txt + актуализация на адрес — Task Report

Дата: 2026-08-27

## Цел

Добавяне на публичен `/llms.txt` за AI системи и актуализация на бизнес адреса в site config.

## Какво е добавено / променено

- Обновен `siteConfig.business.address` на: `ж.к. Младост 2, бл.210, вх.3, София`
- Нов route `app/llms.txt/route.ts` → `/llms.txt` (`text/plain; charset=utf-8`)
- Builder `lib/seo/llms-txt.ts` генерира съдържанието от `siteConfig` + `getSiteUrl()`
- Unit тест `tests/llms-txt.test.ts`

## URL

- Публичен файл: `/llms.txt`
- `robots.txt` вече съществува чрез `app/robots.ts` (не е пипан в тази задача)

## Променени файлове

- `config/site.ts`
- `lib/seo/llms-txt.ts`
- `app/llms.txt/route.ts`
- `tests/llms-txt.test.ts`
- `docs/task-reports/2026-08-27-llms-txt.md`

## Проверки

- `npm run typecheck` — PASS
- `npx tsx --test tests/llms-txt.test.ts` — PASS

## Бележки

- `D:\store-template` е портнат отделно (виж template report)
- Scratch файлове (`.tmp-*`, `.codex-handoff.md`) не са част от задачата

## Production deployment

- **Feature commit:** `962ac8c`
- **Merge commit:** `0a8d840` (Merge pull request #25)
- **Branch / PR:** `codex/llms-txt` → `main` — https://github.com/vemidi-dev/vemidi-store/pull/25 MERGED
- **Production deploy:** https://vemidi-store-nikth6bxi-ve-mi-di.vercel.app — **Ready**
- **Production alias:** https://vemidi-store-ve-mi-di.vercel.app/llms.txt — **200** `text/plain; charset=utf-8`
- **Проверен URL:** `/llms.txt` съдържа `# VeMiDi crafts`, `/sitemap.xml`, `/robots.txt`, `/produkti`, адреса `ж.к. Младост 2, бл.210, вх.3, София`; няма `/admin` или `/checkout`
- **Бележка за домейн:** `vemidicrafts.com` не резолвира. Конфигурираният домейн е `vemidi-crafts.com`. Към момента на проверката `https://vemidi-crafts.com/llms.txt` връщаше 404 (`/_not-found`), докато production alias работи. `robots.txt` **не е променян** (`app/robots.ts` непроменен).
- **Typecheck:** PASS
- **llms test:** PASS
- **Production deploy:** направен (merge към main → Vercel Production Ready)

## Custom domain verification

Дата: 2026-08-29

### Причина за 404

`vemidi-crafts.com` и `www.vemidi-crafts.com` бяха закачени като Vercel alias към **стар** production deployment:

- стар target: `vemidi-store-f9myoml6h-ve-mi-di.vercel.app` (без `/llms.txt` → 404)
- текущ production: `vemidi-store-f410befcl-ve-mi-di.vercel.app` / `vemidi-store-ve-mi-di.vercel.app` (с `/llms.txt` → 200)

Кодът в `main` вече съдържаше `app/llms.txt/route.ts`; проблемът беше само domain→deployment mapping, не липсващ код.

### Какво беше направено във Vercel

Пренасочени aliases към текущия production deploy:

```text
vercel alias set vemidi-store-f410befcl-ve-mi-di.vercel.app vemidi-crafts.com
vercel alias set vemidi-store-f410befcl-ve-mi-di.vercel.app www.vemidi-crafts.com
```

Няма code/functional промени за тази поправка.

### Резултати след поправката

| URL | Status | Content-Type |
|-----|--------|--------------|
| https://vemidi-crafts.com/llms.txt | **200** | `text/plain; charset=utf-8` |
| https://www.vemidi-crafts.com/llms.txt | **200** | `text/plain; charset=utf-8` |
| https://vemidi-crafts.com/robots.txt | **200** | `text/plain` |
| https://vemidi-crafts.com/sitemap.xml | **200** | `application/xml` |
| https://vemidi-store-ve-mi-di.vercel.app/llms.txt | **200** | `text/plain; charset=utf-8` |

`/llms.txt` на custom domain съдържа `# VeMiDi crafts`, адреса `ж.к. Младост 2, бл.210, вх.3, София`, `/sitemap.xml`, `/produkti`; няма `/admin` или `/checkout`.

### Deployment status

- Custom domains вече сочат към: `vemidi-store-f410befcl-ve-mi-di.vercel.app`
- Production alias: `vemidi-store-ve-mi-di.vercel.app` — Ready
- Template не е пипан
